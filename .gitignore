node_modules
.cache
.env
.next
data/raw
.env
site/.next
site/out
site/node_modules

# Calibration is per-machine working state until it is scored. The filled-in JSONL is the
# record worth keeping; the sheets and the screening pack are scratch.
data/calibration/*.md
data/calibration/*-strata.json
data/calibration/*-prescreen/
site/lib/core
