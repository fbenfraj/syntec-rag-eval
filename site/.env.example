# Cloudflare Web Analytics — counts ARRIVALS only. Whether a visitor ran a demo question is
# recorded server-side in `demo_usage`; read that with `npm run readout` from the repository
# root, and treat it as the authoritative number.
#
# Inlined at build time, so a redeploy is required after setting it.
NEXT_PUBLIC_CF_BEACON_TOKEN=
